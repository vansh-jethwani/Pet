const fs=require('fs'); const path='client/pages/Breeding.tsx'; let s=fs.readFileSync(path,'utf8'); let before=s.match(/owner:\"([^\"]+)\"/); console.log('before', before && before[0]); let replaced=s.replace(/owner:\"([^\"]+)\"/g,(m,o)=>{const canonical='owner_'+o.toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,''); return `ownerId:\"${canonical}\", owner:\"${o}\"`;}); let after=replaced.match(/ownerId:\"([^\"]+)\", owner:\"([^\"]+)\"/); console.log('after', after && after[0]); fs.writeFileSync(path,replaced,'utf8'); console.log('done');